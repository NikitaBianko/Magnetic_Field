<Window x:Class="Magnetic_Line_Drawer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Magnetic_Line_Drawer"
        mc:Ignorable="d"
        Title="MainWindow" Height="1030" Width="1250">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>

        <Canvas Grid.Column="0" Background="Lavender" Height="1000" Width="1000" x:Name="canvas" />
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition />
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="2" x:Name="RunSimulationBtn" Click="RunSimulationBtn_Click">Run Simulation</Button>
        </Grid>
    </Grid>
</Window>
